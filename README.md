# WanderLust

Express + Mongoose app to manage travel listings.

## Run locally
1. `npm install`
2. Create a `.env` with `MONGO_URL` (do not commit `.env`)
3. `npm start` or `node app.js` or `nodemon app.js`

## Notes
- Uses EJS templates
- Joi validation for listings
